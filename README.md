# bulk-file-renamer

Windows desktop app for batch-renaming files by composable rules. WPF + MVVM
+ a pure rule-engine library that's covered by xUnit.

_Window screenshot, build instructions, and feature list land in this file
once the implementation is wired up._
